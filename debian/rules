#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS = optimize=-lto

%:
	dh $@ --with autoreconf

override_dh_auto_clean:
	dh_autoreconf || true
	dh_auto_clean || true

override_dh_auto_test:
	$(MAKE) check || true
